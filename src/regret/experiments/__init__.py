"""Experiment pipeline package.

This module provides the experiment pipeline infrastructure including
validation, orchestration, and utilities.

Note:
    Registries have been moved to `regret._registry`. Import from there:

        from regret._registry import (
            ALGORITHM_REGISTRY,
            PROBLEM_REGISTRY,
            COOLING_REGISTRY,
        )
"""
